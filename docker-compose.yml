services:
    postgres:
        image: postgres:18.1
        environment:
            - POSTGRES_USER=postgres
            - POSTGRES_PASSWORD=password
            - POSTGRES_DB=habbit-tracker
        volumes:
            - postgres-db-volume:/var/lib/postgresql
        ports:
            - 5432:5432

volumes:
    postgres-db-volume: